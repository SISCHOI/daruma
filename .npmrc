# Publish and package queries go to the official registry; the user-level
# .npmrc points at npmmirror for fast installs, which cannot serve publishes
# and 404s on unpublished names.
registry=https://registry.npmjs.org/
