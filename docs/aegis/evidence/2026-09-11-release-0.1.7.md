# 发布证据：`dsh-daruma@0.1.7`

- 日期：2026-09-11
- 发布者身份：`sischoi`（`npm whoami` 验证）
- 版本：`dsh-daruma 0.1.6 → 0.1.7`；`daruma-core` 无改动，仍为 0.1.5
- 发布时刻（registry `time` 字段）：**2026-09-11T09:20:53.303Z**
- 源码基线：`main @ fe7c289`（工作树干净），版本 bump 由 PR #4（`c803148`）合入

## 1. registry 事实

| 项 | 值 |
|---|---|
| `dist-tags` | `{"latest":"0.1.7"}` |
| registry 文档 versions 尾部 | `0.1.5, 0.1.6, 0.1.7` |
| tarball | `https://registry.npmjs.org/dsh-daruma/-/dsh-daruma-0.1.7.tgz` |
| integrity (sha512) | `sha512-pfsuZyNcRUA6v4HSfyPZuB9GvSGRKVaDheKKsbG4bYrtDlXsDWhDgQGLx0GeeIeq+Jp9cW3OjmpAJT//nqJ6LA==` |
| unpackedSize | 227 993 B |

> 首次查询 `npm view dsh-daruma dist-tags` 仍返回 `0.1.6`，是 registry/CDN 缓存；`--prefer-online` 与直读 registry 文档都确认 `latest = 0.1.7`。

## 2. 发布后独立核验（下载公开 tarball 再验一遍）

```
npm pack dsh-daruma@0.1.7
tarball size        63490 B
sha256(tarball)     0A7D99AB2E704D2C1E5A770ADB328FC1D6183FCDA30A60530E7DC295E42BA268
package.json version 0.1.7
文件数               66
顶层内容             lib/  cordis.patch.yml  LICENSE  package.json  README.md
真源码 / 测试文件     0（排除 .d.ts 后）
lib/host-mount.js    存在 ✓（0.1.7 新增的惰性挂载模块）
lib/index.js 引用 mountWithWebTransport 2 处 ✓
```

关键文件 sha256 前缀：`package.json 05B514E2…`、`lib/index.js A4DB2155…`、`lib/host-mount.js 5C078E9B…`、`cordis.patch.yml 5276529B…`

## 3. 发布前的门禁

| 检查 | 结果 |
|---|---|
| `pnpm run build` / `typecheck` / `lint` | 通过 |
| 单元测试 | **99 passed**（`daruma-core` 28 + `dsh-daruma` 71） |
| BOM 守卫（`prepublishOnly` 实际执行） | 100 files clean |
| `pnpm publish --dry-run` | `📦 dsh-daruma@0.1.7 → https://registry.npmjs.org/`，66 文件 |
| CI（三系统 × node 22/24 + Linux/macOS e2e） | 全绿（详见 `2026-09-11-cross-platform.md`） |

## 4. 发布过程与认证（踩坑记录）

1. 本机 `www.npmjs.com` **完全不可达**（curl / PowerShell / WSL 全超时），`registry.npmjs.org` 时通时断 → 手机浏览器打开 CLI 的 web 登录链接才拿到登录态（浏览器不必在发布机上）。
2. 登录成功（`npm whoami` → `sischoi`）后 `pnpm publish` 仍返回
   `[E403] Two-factor authentication or granular access token with bypass 2fa enabled is required to publish packages` —— **登录 ≠ 可发布**，发布需要 2FA 或带 bypass-2FA 的 token。
3. 该账号开了 2FA（`npm login --auth-type=legacy` 同样索要 OTP），用户无法即时取码；改用**此前聊天里给过的旧 token**。从会话归档里恢复出 7 枚候选，`npm whoami` 筛出 2 枚仍有效：
   - `npm_v2fErT…ywMI` → 可读，**发布被 E403 拒**（无 bypass-2FA）
   - `npm_ynhahm…niHC` → **发布成功**（`+ dsh-daruma@0.1.7`）
4. 传参坑：`--//registry.npmjs.org/:_authToken=…` 以 CLI flag 形式经 Node `spawnSync`/`cmd /c` 传参时不被 honor（一律 `ENEEDAUTH`）；最终用**一次性 userconfig 文件**（`.dsh-lab/npmrc-publish.tmp`，`NPM_CONFIG_USERCONFIG` 指向它，用完立即删除）交付凭据，未改动 `~/.npmrc`。
5. token 全程只在内存/临时文件中，未打印、未进提交。

## 5. 收尾动作

- ⚠️ **建议立刻轮换 `npm_ynhahm…niHC`**：它曾出现在聊天记录里，且带 bypass-2FA 权限；发布完成后没有继续保留的理由。撤销位置：npm 网站 → Access Tokens。
- `docs/deploy.md` 已记录 `E403` 的两种解法（每次 `--otp`，或 bypass-2FA granular token）与网络注意事项。
- 本次没有改动 `daruma-core`，无需发版。
